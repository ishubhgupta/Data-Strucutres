#include<iostream>
#include<vector>
using namespace std;

int main(){
    vector<int> vec1, hTable;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        int num;
        cin >> num;
        vec1.push_back(num);
    }
    for(int x:vec1){
        hTable.insert();
    }
    for(int x:hTable){
        cout<<x<<" ";
    }

    
}